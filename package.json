{
  "name": "my-study-notes-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "predev": "node scripts/generate-notes.mjs",
    "dev": "vite",
    "prebuild": "node scripts/generate-notes.mjs",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.1.7"
  }
}
